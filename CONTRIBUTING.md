# Contributing

Open an issue describing the invariant or operational behavior being changed. Keep contracts and
rules versioned, add an executable failure case, run `make verify`, and update the relevant ADR if
the publication, replay, identity or privacy semantics change. Never add customer data, secrets or
confidential infrastructure values.
